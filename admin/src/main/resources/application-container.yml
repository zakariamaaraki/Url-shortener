server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka

spring:
  application:
    name: admin

management:
  endpoints:
    web:
      exposure:
        include: health, mappings, metrics